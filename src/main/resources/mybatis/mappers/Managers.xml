<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mybatis.mapper.ManagerMapper">
  <select id="findAll" resultType="com.football.model.Manager">
    SELECT * FROM managers
  </select>

  <insert id="insert" parameterType="com.football.model.Manager">
    INSERT INTO managers (first_name, last_name, nationality, date_of_birth, experience_years)
    VALUES (#{firstName}, #{lastName}, #{nationality}, #{dateOfBirth}, #{experienceYears})
  </insert>
</mapper>
