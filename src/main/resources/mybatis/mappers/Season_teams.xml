<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mybatis.mapper.SeasonTeamsMapper">
  <select id="findAll" resultType="com.football.model.SeasonTeams">
    SELECT * FROM season_teams
  </select>

  <insert id="insert" parameterType="com.football.model.SeasonTeams">
    INSERT INTO season_teams ()
    VALUES ()
  </insert>
</mapper>
