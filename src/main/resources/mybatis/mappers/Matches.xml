<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.football.mybatis.mapper.MatcheMapper">
  <select id="findAll" resultType="com.football.model.Matche">
    SELECT * FROM matches
  </select>

  <insert id="insert" parameterType="com.football.model.Matche">
    INSERT INTO matches (season_id, home_team_id, away_team_id, match_date, stadium_id)
    VALUES (#{seasonId}, #{homeTeamId}, #{awayTeamId}, #{matchDate}, #{stadiumId})
  </insert>
</mapper>
